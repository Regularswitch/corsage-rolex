import Cmp from "./cmp.js"

const cmp = new Cmp()
cmp.slugHome('rolex-na-corsage')
cmp.slugNewModels('novos-modelos')
cmp.slugMaintenance('manutencao')
cmp.slugHistory('nossa-historia-com-a-rolex')
cmp.slugContact('contato-rolex')
cmp.dataLayer()
